/*Javascript for calcating largest palindrom */


/*This function will be called for actual calculation of the problem. The name of this function must be always calculate(inputs)*/
function calculate(input1,input2,inputN)
{
	
	return output;
}

/*This function will be called for validating inputs before calculation.The name of this function must be always validate(inputs)*/

function validate(inputs)
{
	/*conditions*/
	if()
		/*any error will be return to problem page*/
		return "error";
	else if()
	{
		return "error";
	}

}	